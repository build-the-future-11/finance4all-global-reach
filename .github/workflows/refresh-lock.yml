name: Refresh dependency lock

on:
  push:
    branches: ["cursor/membership-security-supabase-fix"]

permissions:
  contents: write

jobs:
  refresh-lock:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: cursor/membership-security-supabase-fix
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm install --package-lock-only
      - name: Apply non-breaking audit fixes to lockfile
        run: npm audit fix --package-lock-only || true
      - name: Verify production dependency audit
        run: npm audit --omit=dev --audit-level=high
      - name: Commit refreshed lockfile
        run: |
          if git diff --quiet -- package-lock.json; then
            echo "package-lock.json already current"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "Refresh lockfile for security patches [skip ci]"
          git push origin HEAD:cursor/membership-security-supabase-fix
